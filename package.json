{
  "name": "youtube-gacha",
  "private": true,
  "type": "module",
  "description": "Dev harness only — the shipped app is plain ES modules with zero dependencies and no build step. Vitest runs the test suite against the pure core.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:report": "node tools/test-report.js"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.10",
    "vitest": "^4.1.10"
  }
}
